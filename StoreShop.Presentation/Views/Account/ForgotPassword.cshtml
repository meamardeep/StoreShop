@model StoreShop.Data.LogOnModel

@{
    Layout = "_LogonLayout";
}


<div class="border shadow rounded-lg container align-items-center mt-5">

    @using (Html.BeginForm("LogOn", "Account", FormMethod.Post))
    {
        <div class="form-group pl-5">
            <h2><span>Password assistance</span></h2>
            <h6>Enter the email address or mobile phone number associated with your storeshop account.</h6>
        </div>

        <div class="form-group pl-5 pr-5">
            <div>
                @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", @required = "true", @placeholder = "User name/ Email" })
                <span style="color:red"> @Html.ValidationMessageFor(m => m.UserName)</span>
            </div>
        </div>


        <div class="form-group pl-5 pr-5">
            <div>
                <button type="submit" id="btnLogIn" class="btn btn-primary form-control" style="width:100%;">Continue</button>
            </div>
        </div>


    }
</div>
